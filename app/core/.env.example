# Environment variables for the FastAPI demo application
# Copy this file into `.env` and adjust values

# --- Database configuration

DB_DRIVER=postgresql
DB_USER=admin_user
DB_PASSWORD=admin_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastapi_demo_db

# --- OAuth2 configuration

OAUTH_HASH_KEY=             # To get a hash key run: openssl rand -hex 32
OAUTH_ALGORITHM=HS256       # or any other algorithm supported by PyJWT
OAUTH_TOKEN_TTL=30          # Token time-to-live in minutes

# --- Pagination settings

DEFAULT_PAGE_SIZE=5
MAX_PAGE_SIZE=20