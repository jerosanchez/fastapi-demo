# Copy this file to .env and adjust values as needed.
# You can leave the default values for local development.
# Remove secrets in production environments.

# --- Secrets (override in production via secret management)

POSTGRES_PASSWORD=admin_password
OAUTH_HASH_KEY=7bdb4bcf0b01f00ca206693965b29145611d9419563630525dc848024c38fc08

# --- Database

POSTGRES_IMAGE=postgres:latest

POSTGRES_HOST=api-db
POSTGRES_PORT=5432
POSTGRES_USER=admin_user
POSTGRES_DB=fastapi_demo_db

# --- Docker network

DOCKER_NETWORK_NAME=internal

# --- Application settings

DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

OAUTH_ALGORITHM=HS256
OAUTH_TOKEN_TTL=30  # Token time-to-live in minutes

DEFAULT_PAGE_SIZE=5
MAX_PAGE_SIZE=20

